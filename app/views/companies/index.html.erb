<h1>Companies </h1>

<% @companies.each do |company| %>
    <div>
        <h1><%= company.name %></h1>
        <img src=<%=company.image_url%>
        <p><%= link_to company.name, company_path(company) %></p>\
    </div>
<% end %>